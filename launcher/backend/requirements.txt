# Build tools (required for Python 3.12 compatibility)
setuptools>=65.5.1

# FastAPI and server
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.5.3
python-multipart==0.0.6

# PyTorch (CPU version for Railway) - pinned for stability
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.3.1+cpu

# Transformers for NLP - compatible with torch 2.1.2
transformers==4.35.2

# Data processing - pinned versions (Python 3.12 compatible)
numpy==1.26.4
pandas==2.2.0
scikit-learn==1.4.0
Pillow==10.2.0

# Utilities - pinned versions
python-dotenv==1.0.0

# Authentication
bcrypt==4.1.2
PyJWT==2.8.0
