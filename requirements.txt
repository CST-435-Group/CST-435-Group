# Root-level requirements.txt for Railway deployment
# Optimized with pinned versions and removed unused dependencies

# Build tools (required for Python 3.12 compatibility)
setuptools>=65.5.1

# FastAPI and server
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.5.3
python-multipart==0.0.6

# PyTorch (CPU version for Railway) - pinned for faster builds
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.5.1+cpu

# Transformers for NLP - pinned version
transformers==4.35.0

# Data processing - pinned versions (Python 3.12 compatible)
numpy==1.26.4
pandas==2.2.0
scikit-learn==1.4.0
Pillow==10.2.0

# Utilities - pinned version
python-dotenv==1.0.0
