# Root-level requirements.txt for Railway deployment
# Optimized with pinned versions and removed unused dependencies

# FastAPI and server
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.5.3
python-multipart==0.0.6

# PyTorch (CPU version for Railway) - pinned for faster builds
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.1.0

# Transformers for NLP - pinned version
transformers==4.35.0

# Data processing - pinned versions
numpy==1.24.3
pandas==2.0.0
scikit-learn==1.3.0
Pillow==10.0.0

# Utilities - pinned version
python-dotenv==1.0.0
